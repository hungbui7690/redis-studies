/*

  > docker-compose up --build -d --remove-orphans
  > http://localhost:3050


  - Start
    > sudo service redis-server start
    > redis-cli

    - ping
    - ping "Hello"







































*/
